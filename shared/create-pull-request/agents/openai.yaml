interface:
  display_name: "create-pull-request"
  short_description: "Create a pull request with template and conventions"
  default_prompt: "Use $create-pull-request to create a pull request from the current branch."
policy:
  allow_implicit_invocation: true
