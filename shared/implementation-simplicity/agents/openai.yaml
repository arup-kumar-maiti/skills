interface:
  display_name: "implementation-simplicity"
  short_description: "Keep code and tests simple and necessary"
  default_prompt: "Use $implementation-simplicity to review changed code and tests or audit a named implementation area for needless complexity."
policy:
  allow_implicit_invocation: true
